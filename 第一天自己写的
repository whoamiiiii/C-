//
//  main.c
//  第一天_韩信点兵
//
//  Created by Haotian Dong on 5/24/19.
//  Copyright © 2019 Haotian Dong. All rights reserved.
//  相传韩信才智过人，从不直接清点自己军队的人数，只要让士兵先后以三人一排、五人一排、七人一排地变换队形，而他每次只掠一眼队伍的排尾就知道总人数了。输入3个非负整数a,b,c ，表示每种队形排尾的人数（a<3,b<5,c<7），输出总人数的最小值（或报告无解）。已知总人数不小于10，不超过100 。
//  输入
//  输入3个非负整数a,b,c ，表示每种队形排尾的人数（a<3,b<5,c<7）。例如,输入：2 4 5
//  输出
//  输出总人数的最小值（或报告无解，即输出No answer）。实例，输出：89

#include <stdio.h>

int main() {
    int i;
    int x, y, z;    //排数, 不大于34, 20, 15
    int a, b, c;    //余数
    
    printf("输入abc:");
    scanf("%d""%d""%d", &a, &b, &c);
    
    for(i = 0; i < 100; i++)
    {
        for(x = 0; x < 34; x++)         //3
        {
            for(y = 0; y < 20; y++)     //5
            {
                for(z = 0; z< 15; z++)  //7
                {
                    if(i == 3 * x + a && i == 5 * y + b && i == 7 * z + c)
                    {
                        printf("人数是%d", i);
                        break;
                    }
                }
            }
        }
    }
    
}
